
import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

final NavBarShadow =
    BoxShadow(color: Colors.black, offset: const Offset(2, 2), spreadRadius: 1.r);

    final defShadow = 
    BoxShadow(color: Colors.black, offset: const Offset(3,3), spreadRadius: 1.r);
